# P6 — COMPLETE ENTERPRISE-GRADE ERP IMPROVEMENT, PERFORMANCE, SECURITY & SCALABILITY UPGRADE

## ROLE

Act as a senior full-stack ERP/POS architect, pharmacy ERP specialist, SaaS multi-tenant architecture expert, database architect, security engineer, performance engineer, DevOps engineer and professional enterprise UI/UX designer.

The existing **MedCare Pharmacy & Healthcare ERP/POS** already contains or is implementing P1, P2, P3, P4 and P5.

P6 is an additional enterprise-level improvement layer.

IMPORTANT:

- Do not break P1, P2, P3, P4 or P5.
- Do not remove any existing working functionality.
- Do not unnecessarily rewrite stable modules.
- Do not change existing business logic unless required for correctness, security, performance or the new requirements.
- Reuse existing components, APIs, database structures and services wherever possible.
- First inspect the existing architecture before modifying anything.
- Implement changes incrementally and safely.
- Preserve existing data.
- Do not create duplicate database records unnecessarily.
- Do not introduce mock/demo data into production functionality.
- Do not hardcode branch IDs, user IDs, medicine IDs or organization IDs.
- All important relationships must use UUIDs and proper foreign keys.
- All new features must respect existing authentication, authorization and branch isolation.

---

# 1. CENTRAL MEDICINE MASTER + BRANCH-SPECIFIC INVENTORY

Separate Medicine Master data from branch-specific inventory.

Medicine Master should contain organization-level information such as:

- Medicine UUID
- Organization UUID
- Medicine name
- Generic/composition
- Brand
- Manufacturer
- Category
- HSN
- Tax information
- Unit
- Pack size
- Barcode
- SKU
- Description
- Other permanent medicine attributes

Branch Inventory should contain:

- Organization UUID
- Branch UUID
- Medicine UUID
- Current quantity
- Minimum stock
- Maximum stock
- Rack/shelf
- Branch-specific pricing where applicable
- Batch
- Expiry
- Purchase information
- Stock status

Example:

Paracetamol:
- Main Branch → 100 units
- Branch 02 → 75 units
- Branch 03 → 40 units

The medicine master may be shared across branches, but inventory must remain completely branch-specific.

Never accidentally combine stock between branches.

A stock quantity from Branch 02 must never appear as Branch 03 stock.

---

# 2. BRANCH-TO-BRANCH STOCK TRANSFER

Create a complete Stock Transfer system.

Authorized users should be able to transfer medicine stock between branches.

Workflow:

Draft
↓
Requested
↓
Approved
↓
Dispatched
↓
In Transit
↓
Received
↓
Completed

Each transfer should contain:

- Transfer UUID
- Organization UUID
- Source Branch UUID
- Destination Branch UUID
- Medicine UUID
- Batch UUID
- Requested quantity
- Approved quantity
- Dispatched quantity
- Received quantity
- Requested by
- Approved by
- Dispatched by
- Received by
- Request date
- Approval date
- Dispatch date
- Receive date
- Notes
- Status
- Audit information

Use proper database transactions.

Prevent:

- Negative stock
- Duplicate transfer processing
- Double receiving
- Unauthorized transfers
- Cross-organization transfers
- Transfer of unavailable stock
- Receiving more stock than dispatched
- Editing completed transfers without authorization

Maintain a complete audit trail.

---

# 3. CENTRAL PURCHASE + BRANCH PURCHASE

Support both:

## Branch Purchase

A branch can independently create and manage its own purchase.

## Central Purchase

Super Admin can create a centralized purchase and distribute received stock to one or multiple branches.

Example:

Central Purchase
↓
1000 Units
↓
Branch 01 → 400
Branch 02 → 300
Branch 03 → 300

Maintain correct relationships between:

- Purchase
- Supplier
- Purchase items
- Batch
- Expiry
- Stock
- Branch allocation
- Payment
- Outstanding balance

Do not create unnecessary duplicate purchase records.

All financial calculations must remain accurate.

---

# 4. CENTRAL SUPPLIER MASTER

Create a proper organization-level supplier master.

A supplier can be:

- Organization-wide
- Branch-specific
- Associated with multiple branches

Supplier master information may be shared, but supplier ledger transactions must remain branch-specific.

Example:

Supplier ABC
↓
Branch 01 Ledger
Branch 02 Ledger
Branch 03 Ledger

Never mix financial transactions between branches.

Supplier balances should be available as:

- Branch-wise balance
- Organization-wide balance for Super Admin
- Paid amount
- Outstanding amount
- Purchase history
- Return history
- Payment history

---

# 5. CROSS-BRANCH CUSTOMER RECOGNITION

Improve customer management so the same customer can optionally be recognized across branches.

Search using:

- Mobile number
- Customer name
- Customer UUID
- Email where applicable

Example:

Customer enters Branch 04
↓
Staff enters mobile number
↓
Existing organization customer detected
↓
Authorized customer information displayed

Prefer an architecture such as:

Organization Customer
+
Branch Customer Relationship

Avoid unnecessary duplicate customer records.

IMPORTANT:

- Respect privacy.
- Respect role permissions.
- Do not expose unnecessary customer information to unauthorized staff.
- Branch staff should only see information allowed by their role.
- Super Admin may access organization-wide information according to authorization.
- Customer financial/credit information must remain protected.

---

# 6. ADVANCED TALLY-STYLE AUTOCOMPLETE SEARCH

Extend the P1 search system into a unified high-performance autocomplete/search engine.

All relevant search boxes throughout the website must support:

- Character-by-character search
- Instant suggestions
- Prefix matching
- Partial matching
- Mobile number search
- Invoice number search
- Medicine search
- Customer search
- Supplier search
- Staff search
- Branch search
- Barcode search
- SKU search
- HSN search
- Batch search
- UUID search where appropriate

Example:

User types:

m

Immediately show matching results.

Then:

me

Immediately update results.

Then:

med

Immediately update results.

Do not require the user to press Enter for normal autocomplete.

Use debouncing/throttling to prevent excessive API requests.

Recommended flow:

User typing
↓
Debounce
↓
Optimized Search API
↓
Database/index search
↓
Ranked suggestions
↓
Instant UI result

The search must remain fast even when the database contains very large amounts of data.

---

# 7. APPLY THE SEARCH SYSTEM TO THE ENTIRE WEBSITE

The P1 search/autocomplete system must be reusable across all relevant search fields.

Apply it to:

- Global search
- POS medicine search
- POS customer search
- Barcode input
- Inventory search
- Medicine master search
- Purchase medicine search
- Supplier search
- Customer search
- Sales history search
- Invoice search
- Expense search
- Staff search
- Branch search
- Reports search
- Batch search
- Stock transfer search
- Purchase order search
- Any future search field

Do not create eight or ten completely different search implementations.

Create a reusable search/autocomplete component and reusable search service.

The component should be configurable according to:

- Entity type
- Search fields
- Permissions
- Current branch
- Organization
- Result limit
- Sorting
- Display fields
- Keyboard navigation
- Mobile behavior

---

# 8. CONTEXT-AWARE SEARCH

Search results must respect the user's current context.

Normal Branch User:

Current Branch
↓
Authorized Search Results

Super Admin:

Current Branch
OR
Selected Branch
OR
All Authorized Branches

When results come from multiple branches, clearly display the branch.

Example:

Paracetamol
Branch 02
Stock: 50

Paracetamol
Branch 04
Stock: 120

Never silently combine records from different branches.

---

# 9. GLOBAL COMMAND CENTER

Create a powerful global command/search center for Super Admin.

Example:

Search anything...

It should be able to find:

- Branches
- Medicines
- Customers
- Suppliers
- Staff
- Invoices
- Purchases
- Expenses
- Stock transfers
- Purchase orders
- Batches
- Reports
- Important settings where permitted

Results should be grouped by entity type.

Example:

MEDICINES
Paracetamol

CUSTOMERS
Rahul Kumar

INVOICES
Invoice 2026-00054

BRANCHES
Deoghar Branch

STAFF
Cashier

Allow keyboard navigation on desktop and touch-friendly navigation on mobile.

---

# 10. CASH REGISTER MANAGEMENT

Add proper branch-level cash register management.

Each branch should be able to maintain:

- Opening cash
- Cash sales
- Cash refunds
- Cash expenses
- Cash deposits
- Cash withdrawals
- Cash adjustments
- Closing cash
- Expected cash
- Actual cash
- Difference
- Cashier
- Shift

Example:

Opening Cash
+
Cash Sales
-
Cash Refunds
-
Cash Expenses
+
Other Cash In
-
Cash Withdrawal
=
Expected Closing Cash

Then compare:

Expected Closing Cash
vs
Actual Closing Cash

Show:

- Shortage
- Excess
- Exact match

Financial calculations must happen server-side.

---

# 11. CASHIER SHIFT MANAGEMENT

Support cashier shifts for each branch.

Example:

Morning Shift
↓
Cashier A

Evening Shift
↓
Cashier B

Night Shift
↓
Cashier C

Each shift should have:

- Shift UUID
- Branch UUID
- Cashier UUID
- Opening time
- Closing time
- Opening cash
- Closing cash
- Total sales
- Cash sales
- Digital sales
- Refunds
- Expenses
- Expected cash
- Actual cash
- Difference
- Status

Prevent the same cashier from accidentally opening multiple active shifts unless explicitly permitted.

---

# 12. APPROVAL WORKFLOW

Introduce role-based approval workflows for sensitive operations.

Possible approval-controlled actions:

- Purchase approval
- Purchase return
- Stock adjustment
- Stock transfer
- Invoice cancellation
- High-value discount
- Expense approval
- Supplier payment
- Large refund
- Manual stock increase
- Manual stock decrease

Example:

Cashier
↓
Request
↓
Manager
↓
Approve

Super Admin should be able to configure approval requirements where appropriate.

Approval actions must be recorded in the audit log.

---

# 13. ROLE-BASED DISCOUNT CONTROL

Create configurable discount limits.

Example:

Cashier:
Maximum discount = 5%

Manager:
Maximum discount = 15%

Super Admin:
Custom/unrestricted according to organization policy

IMPORTANT:

Discount restrictions must be enforced on the backend.

Never rely only on frontend validation.

A user must not be able to bypass the restriction by modifying the browser request.

Record:

- Requested discount
- Approved discount
- Requested by
- Approved by
- Reason
- Timestamp
- Invoice UUID

---

# 14. SOFT DELETE + RECOVERY SYSTEM

Do not permanently delete important business records by default.

Use states such as:

Active
↓
Archived
↓
Recoverable

Apply appropriate soft-delete/recovery behavior to:

- Medicines
- Customers
- Suppliers
- Staff
- Branches
- Purchase records where legally/technically appropriate
- Other important master records

Never allow a soft-deleted record to silently appear in normal operational searches.

Provide authorized recovery functionality.

Financial records should generally remain immutable rather than being deleted.

---

# 15. COMPLETE AUDIT TRAIL

Create a centralized audit log.

Track important operations such as:

- Login
- Logout
- Create
- Update
- Delete/archive
- Restore
- Invoice creation
- Invoice cancellation
- Purchase creation
- Purchase modification
- Stock adjustment
- Stock transfer
- Customer modification
- Supplier modification
- Staff modification
- Branch creation
- Branch update
- Permission change
- Discount approval
- Cash register operations
- Settings changes
- Branding changes

Audit entry should contain:

- Audit UUID
- Organization UUID
- Branch UUID where applicable
- User UUID
- Action
- Entity type
- Entity UUID
- Previous value where appropriate
- New value where appropriate
- Timestamp
- IP/device information where legally appropriate
- Request/reference ID

Audit logs should not be casually editable or deletable.

---

# 16. ENTITY ACTIVITY TIMELINE

Important entities should have an activity timeline.

Example:

Medicine created
↓
Purchase added
↓
Batch created
↓
Stock increased
↓
Sale created
↓
Stock reduced
↓
Return created

Apply similar timelines to:

- Customers
- Suppliers
- Medicines
- Invoices
- Purchases
- Branches
- Staff
- Stock transfers

This should help Super Admin and authorized managers quickly understand what happened.

---

# 17. NOTIFICATION CENTER

Create a centralized notification center.

Notifications may include:

- Low stock
- Out of stock
- Near expiry
- Expired batches
- Pending purchase
- Pending approval
- Pending stock transfer
- Supplier payment due
- Customer credit due
- Cash mismatch
- Failed backup
- System error
- Security alert
- New staff created
- Branch created
- Important system event

Notifications must respect role and branch permissions.

Do not show Branch 02 alerts to an unauthorized Branch 03 user.

---

# 18. SMART DASHBOARD

Improve dashboards so information is branch-aware.

Branch dashboard:

- Today's sales
- Today's profit where available
- Current stock value
- Low stock
- Expiring stock
- Pending purchases
- Customer credit
- Supplier payable
- Cash status
- Recent invoices

Super Admin dashboard:

- Total branches
- Total sales
- Branch-wise sales
- Organization stock value
- Low stock across branches
- Expiry across branches
- Supplier payables
- Customer receivables
- Branch performance
- Cash status
- Staff activity
- System health

Never calculate organization-wide metrics by simply summing incompatible or duplicated records.

Use correct aggregation logic.

---

# 19. BRANCH-WISE REPORTING

Every major report should support:

- Current branch
- Selected branch
- Multiple selected branches
- All branches for Super Admin

Reports should clearly identify the branch.

Examples:

- Sales report
- Purchase report
- Inventory report
- Expiry report
- Profit report
- Expense report
- Customer credit report
- Supplier payable report
- Cash report
- Tax report
- Stock transfer report

Branch users must never be able to access unauthorized branch reports.

---

# 20. FEATURE FLAGS PER BRANCH

Allow Super Admin to configure which features are enabled for each branch.

Example:

Branch 02:

POS ✓
Credit Sale ✓
Wholesale ✕
Stock Transfer ✓
Expense Module ✓
Central Purchase ✕

Branch 03:

POS ✓
Credit Sale ✕
Wholesale ✓
Stock Transfer ✓
Expense Module ✓
Central Purchase ✓

Feature restrictions must be enforced both:

- Frontend
- Backend

Hiding a button is not sufficient.

---

# 21. BRANCH CONFIGURATION

Each branch should support configurable:

- Business name
- Address
- Phone
- Email
- GST information where applicable
- Pharmacy license information where applicable
- Logo/branding inherited from organization or overridden where permitted
- Invoice prefix
- Invoice numbering
- Tax configuration where applicable
- Receipt settings
- Printer settings
- POS settings
- Payment methods
- Discount rules
- Credit sale rules
- Staff permissions
- Feature flags

Maintain organization-level defaults with optional branch-level overrides.

---

# 22. BRANCH-WISE INVOICE NUMBERING

Prevent invoice-number collisions across branches.

Use branch-specific numbering.

Example:

MAIN-2026-000001
BR02-2026-000001
BR03-2026-000001

Invoice number generation must be:

- Atomic
- Transaction-safe
- Unique
- Collision-resistant
- Branch-aware

Never generate invoice numbers only on the frontend.

---

# 23. CENTRALIZED BUSINESS SETTINGS + BRANCH OVERRIDES

Create a hierarchy:

Organization Defaults
↓
Branch Overrides
↓
User/Role Permissions where applicable

For example:

Organization default tax setting
↓
Branch-specific override

Organization invoice design
↓
Branch-specific invoice setting

This prevents unnecessary duplicate configuration.

---

# 24. PERFORMANCE OPTIMIZATION

The entire application should be optimized for very high performance.

Target:

- Fast initial page rendering
- Fast route transitions
- Fast API responses
- Fast database queries
- Fast search
- Fast POS interactions
- Minimal unnecessary re-renders
- Minimal network requests
- Efficient caching
- Efficient pagination

Do not promise impossible guaranteed millisecond responses for every request under every real-world condition.

Instead, architect the system to minimize latency and remain responsive under heavy load.

---

# 25. HIGH-TRAFFIC ARCHITECTURE

Design the application to scale toward heavy concurrent traffic.

The architecture should support horizontal scaling where appropriate.

Use:

- Stateless application servers where possible
- Efficient database connections
- Connection pooling
- Caching
- Indexed queries
- Pagination
- Lazy loading
- Background jobs
- Queue processing
- Rate limiting
- Request deduplication
- Efficient API payloads
- CDN/static asset caching where appropriate

Avoid:

- N+1 database queries
- Loading entire tables into the browser
- Repeated identical API calls
- Huge JSON responses
- Unnecessary polling
- Blocking long-running operations

---

# 26. DATABASE PERFORMANCE

Audit all important database queries.

Add proper indexes for frequently searched fields such as appropriate combinations of:

- Organization UUID
- Branch UUID
- Medicine UUID
- Customer UUID
- Supplier UUID
- Invoice UUID
- Invoice number
- Mobile number
- Barcode
- SKU
- Batch UUID
- Created date
- Status

Do not blindly index every column.

Use proper query planning and database profiling.

Large tables must use pagination.

Never fetch thousands or millions of rows when the UI needs only 20–50 records.

---

# 27. SEARCH PERFORMANCE AT SCALE

The P1 autocomplete search must remain usable when data becomes very large.

Implement:

- Debouncing
- Minimum search characters where appropriate
- Result limits
- Pagination
- Proper indexes
- Prefix search
- Full-text search where appropriate
- Cached common searches where useful
- Server-side filtering
- Request cancellation
- Race-condition protection

If the user types:

m
me
med
medi

An older API response must not overwrite a newer result.

Always display the latest valid search result.

---

# 28. API PERFORMANCE

Audit APIs for:

- Duplicate calls
- Excessive payload size
- Slow queries
- Unnecessary joins
- Missing indexes
- N+1 queries
- Repeated calculations
- Missing caching opportunities

Use:

- Pagination
- Filtering
- Sorting
- Selective fields
- Compression where appropriate
- Caching
- Background processing

Do not expose unnecessary database fields to the frontend.

---

# 29. BACKGROUND JOB SYSTEM

Long-running operations should not block the user's request.

Potential background jobs:

- Large report generation
- PDF generation
- Large exports
- Backup
- Data import
- Bulk stock operations
- Notifications
- WhatsApp processing where appropriate
- Analytics aggregation

Workflow:

User Request
↓
Create Job
↓
Queue
↓
Worker
↓
Process
↓
Store Result
↓
Notify User

The user should be able to continue using the ERP.

---

# 30. BACKUP SYSTEM

Create a proper automated backup system.

Support:

- Automatic scheduled backups
- Backup history
- Backup status
- Backup verification
- Failed backup detection
- Restore process
- Backup retention policy
- Secure backup storage

Do not claim a backup is successful unless the backup operation has actually completed and been verified.

Super Admin should be able to see:

Last successful backup
Last failed backup
Backup size
Backup timestamp
Backup status

---

# 31. DISASTER RECOVERY

Design for recovery from:

- Database failure
- Server failure
- Deployment failure
- Accidental deletion
- Data corruption
- Application crash

Maintain:

- Recovery strategy
- Backup strategy
- Restore strategy
- Migration rollback strategy
- Disaster recovery documentation

Never make destructive database migrations without a safe migration strategy.

---

# 32. OFFLINE / POOR INTERNET POS SUPPORT

Where technically safe, improve POS resilience for poor internet connectivity.

Support:

- Temporary local cart state
- Offline-safe UI state
- Transaction queue where appropriate
- Automatic synchronization
- Sync status
- Retry mechanism
- Conflict detection

IMPORTANT:

Do not create duplicate invoices or duplicate stock deductions during synchronization.

Financial and inventory synchronization must use transaction-safe server-side logic.

The system must clearly tell the user whether a transaction is:

- Pending Sync
- Synced
- Failed
- Requires Review

---

# 33. PAYMENT STATUS RELIABILITY

Payment operations must be idempotent.

If the same request is accidentally submitted twice, the system must not create two payments.

Use:

- Idempotency keys
- Unique transaction references
- Server-side validation
- Transactional processing

This is especially important for:

- Invoice payments
- Refunds
- Supplier payments
- Customer credit payments

---

# 34. SECURITY HARDENING

Perform a complete security review.

Verify:

- Authentication
- Authorization
- Role-based access
- Branch isolation
- Organization isolation
- API authorization
- Input validation
- SQL injection protection
- XSS protection
- CSRF protection where applicable
- Secure cookies
- Session handling
- Password security
- Rate limiting
- Sensitive-data protection
- Secure file uploads
- Secure API responses

Never trust:

- Frontend role
- Frontend branch ID
- Frontend organization ID
- Hidden fields
- Client-side permission checks

All authorization must be validated server-side.

---

# 35. MULTI-TENANT DATA ISOLATION

Treat organization isolation as a critical security boundary.

Every organization-owned record must be correctly associated with the organization.

Every branch-owned record must be correctly associated with the branch.

The system must prevent:

Organization A
↓
accessing
↓
Organization B data

and:

Branch 02
↓
accessing
↓
Branch 03 data without authorization

Apply isolation at:

- API layer
- Service/business layer
- Database query layer
- Authorization layer

Do not rely only on frontend filtering.

---

# 36. SUPER ADMIN SECURITY

Super Admin has powerful permissions, so protect this role carefully.

Require appropriate authorization for:

- Creating branches
- Deleting/archiving branches
- Managing staff
- Changing roles
- Changing permissions
- Viewing organization-wide data
- Accessing another branch
- Changing business settings
- Changing branding
- Performing sensitive financial actions

Every sensitive Super Admin action must be audited.

---

# 37. BRANCH ACCESS SWITCHING

Super Admin should be able to switch between branches without creating a second login.

Example:

Super Admin
↓
Select Branch
↓
Branch 04
↓
Enter Branch Workspace

The system must change the operational context safely.

The URL/routing should use UUID-based identifiers as defined in P4/P5.

Never trust a branch UUID supplied by the client without validating that the Super Admin has authorization for that branch.

When Super Admin switches branch, clearly display the active branch.

Example:

Active Branch: Deoghar Branch

Avoid accidentally performing a transaction in the wrong branch.

For sensitive actions, optionally require an additional confirmation showing:

Current Branch
↓
Action
↓
Target Branch

---

# 38. STAFF MANAGEMENT

Improve staff management with:

- Staff UUID
- Organization UUID
- Branch UUID
- Role
- Permissions
- Status
- Login status
- Last login
- Assigned branch
- Multiple-branch assignment where appropriate
- Created date
- Updated date

Support role templates:

- Super Admin
- Branch Admin/Manager
- Pharmacist
- Cashier
- Accountant
- Inventory Staff
- Custom Roles

Permissions should be granular.

Example:

POS:
View ✓
Create ✓
Edit ✕
Cancel ✕
Refund ✕

Inventory:
View ✓
Adjust ✕
Transfer ✓

---

# 39. MULTI-BRANCH STAFF ASSIGNMENT

Allow authorized users to assign a staff member to one or multiple branches where appropriate.

Example:

Staff A:

Main Branch ✓
Branch 02 ✓
Branch 03 ✕

The active branch context should determine what the staff member can operate.

Do not allow access to a branch merely because the staff record exists in the organization.

Explicit authorization is required.

---

# 40. SYSTEM HEALTH DASHBOARD

Create a Super Admin System Health section.

Display where technically available:

- API health
- Database health
- Database latency
- Average API response time
- Error rate
- Active sessions
- Active users
- Active branches
- Background jobs
- Queue status
- Backup status
- Storage usage
- Cache status
- Recent system errors

Do not expose sensitive infrastructure information to normal branch users.

---

# 41. ERROR MONITORING

Create a centralized error-monitoring architecture.

Record useful technical information such as:

- Error ID
- Request ID
- Timestamp
- API endpoint
- User UUID where appropriate
- Organization UUID
- Branch UUID
- Error category
- Safe error message
- Stack trace in protected server logs

Do not expose stack traces, database errors or secrets to end users.

Show users a clean message such as:

Something went wrong. Please try again.

Provide a reference ID for support.

---

# 42. REQUEST ID / TRACE ID

Each important API request should have a trace/request ID.

Example:

Request ID:
REQ-xxxxxxxx

Use it for:

- Debugging
- Error tracking
- Support
- Logs
- API tracing
- Background jobs

This will make production troubleshooting much easier.

---

# 43. REAL-TIME UPDATES WHERE USEFUL

Use real-time mechanisms only where they provide actual value.

Potential real-time events:

- New stock transfer
- Transfer received
- Approval request
- Low-stock alert
- Staff status
- Important notification
- POS synchronization
- Background job completion

Do not create unnecessary WebSocket connections for every page.

Use polling only where appropriate.

---

# 44. DATA VALIDATION

All important forms must have:

- Client-side validation
- Server-side validation
- Type validation
- Required field validation
- Range validation
- Duplicate detection
- Business-rule validation

Never rely only on browser validation.

---

# 45. IMPORT / EXPORT SYSTEM

Improve large-data import/export.

Support appropriate imports for:

- Medicines
- Opening stock
- Customers
- Suppliers
- Staff where authorized

Exports may include:

- Sales
- Purchases
- Inventory
- Customers
- Suppliers
- Reports

Large exports should use background jobs rather than blocking the browser.

Provide:

- Validation report
- Successful records
- Failed records
- Error reasons
- Downloadable result

Never partially import data silently.

---

# 46. DATA CONSISTENCY

Financial and inventory operations must be transactional.

For example, invoice creation should correctly coordinate:

Invoice
+
Invoice Items
+
Payment
+
Stock Deduction
+
Customer Ledger
+
Audit Log

If a critical operation fails, do not leave the database in a half-completed state.

Use proper transactions and rollback logic.

---

# 47. STOCK LEDGER

Maintain a proper stock movement ledger.

Every stock change should have a reason.

Examples:

Purchase
Sale
Sales Return
Purchase Return
Stock Transfer Out
Stock Transfer In
Manual Adjustment
Opening Stock
Expired Stock
Damaged Stock

Each movement should contain:

- Stock movement UUID
- Organization UUID
- Branch UUID
- Medicine UUID
- Batch UUID
- Quantity change
- Previous quantity
- New quantity
- Movement type
- Reference UUID
- User UUID
- Timestamp

This becomes the source of truth for inventory auditing.

---

# 48. PROFIT & LOSS DATA INTEGRITY

Profit calculations must not simply be based on selling price minus purchase price if the pharmacy uses batches, discounts, returns, taxes or different cost structures.

Use the correct existing accounting/business rules.

Clearly distinguish:

- Gross sales
- Discounts
- Returns
- Net sales
- Cost of goods sold
- Gross profit
- Expenses
- Net profit

Do not show a misleading profit figure.

---

# 49. CUSTOMER CREDIT / RECEIVABLE MANAGEMENT

Improve credit ledger.

Track:

- Customer UUID
- Branch UUID
- Invoice
- Credit amount
- Payment
- Outstanding amount
- Payment date
- Payment method
- Collected by
- Reference

Support:

- Branch-wise outstanding
- Customer-wise outstanding
- Organization-wide outstanding for authorized Super Admin
- Payment history
- Aging where appropriate

Do not mix branch financial data incorrectly.

---

# 50. SUPPLIER PAYABLE MANAGEMENT

Track:

- Purchase amount
- Paid amount
- Return amount
- Outstanding amount
- Payment history
- Branch
- Supplier
- Invoice/reference
- Payment date
- Payment method

Provide:

- Branch-wise payable
- Supplier-wise payable
- Organization-wide payable for Super Admin

---

# 51. MOBILE + TABLET + DESKTOP RESPONSIVENESS

All P6 functionality must work properly on:

- Android phones
- iPhones
- Android tablets
- iPads
- Desktop
- Laptop

Do not simply shrink the desktop UI.

Use responsive layouts appropriate for each device.

POS should remain usable with touch.

Tables should become mobile-friendly cards or horizontally scrollable data views where appropriate.

Autocomplete dropdowns must not be clipped by viewport boundaries.

Dialogs and sheets must work correctly on iOS Safari.

---

# 52. IOS SAFARI COMPATIBILITY

Pay special attention to:

- Horizontal scrolling
- Touch scrolling
- Fixed headers
- Bottom sheets
- Modals
- Dropdowns
- Search dropdowns
- Input focus
- Keyboard behavior
- Safe areas
- 100vh issues
- Sticky elements
- Overflow containers

Do not introduce CSS that works on Chrome but breaks Safari.

Test all important workflows on iOS Safari.

---

# 53. PROFESSIONAL UI CONSISTENCY

All new P6 interfaces must follow the professional UI system created in P3.

Do not introduce:

- Random styles
- Random colors
- Random card designs
- Emoji icons
- AI-looking decorative icons
- Inconsistent spacing
- Inconsistent typography

Use a centralized design system.

Maintain consistent:

- Typography
- Spacing
- Borders
- Radius
- Shadows
- Buttons
- Inputs
- Tables
- Modals
- Tabs
- Dropdowns
- Alerts
- Status badges

---

# 54. DARK MODE + LIGHT MODE

All P6 features must work perfectly in both:

- Light mode
- Dark mode

Do not create hardcoded white/black backgrounds that break the opposite theme.

Use semantic design tokens.

Check:

- Text contrast
- Input contrast
- Dropdown contrast
- Tables
- Modals
- Charts
- Status indicators
- Notifications
- Search results
- POS
- Reports
- Settings

No page should look unfinished in either theme.

---

# 55. ACCESSIBILITY

Improve accessibility wherever possible.

Support:

- Keyboard navigation
- Focus states
- Screen-reader-friendly labels
- Semantic HTML
- Accessible buttons
- Accessible form labels
- Sufficient contrast
- Touch-friendly controls
- Reduced-motion preferences where appropriate

Do not use icons alone when the meaning is ambiguous.

---

# 56. URL / ROUTING SECURITY

Continue the UUID-based routing architecture from P4/P5.

Never expose sensitive business names, medicine names, customer names or other mutable names as identifiers where UUID routing is required.

Example:

website.com/medicine/<medicine-uuid>

instead of:

website.com/medicine/paracetamol

Apply the same principle to appropriate entities:

- Medicine
- Customer
- Supplier
- Invoice
- Purchase
- Branch
- Staff
- Stock transfer
- Purchase order
- Reports where appropriate

IMPORTANT:

UUID in the URL is not itself authorization.

Every request must still validate:

- Authentication
- Organization ownership
- Branch authorization
- Role permissions

---

# 57. ROUTE STATE PRESERVATION

When navigating between pages:

- Preserve relevant filters where appropriate
- Preserve pagination where useful
- Preserve branch context
- Preserve search state where appropriate
- Avoid unnecessary page reloads
- Avoid losing unsaved form data accidentally

For destructive navigation, warn the user if there is unsaved work.

---

# 58. POS SPEED OPTIMIZATION

POS should prioritize speed and keyboard/touch efficiency.

Optimize:

- Medicine search
- Barcode scanning
- Customer selection
- Quantity entry
- Discount entry
- Payment selection
- Invoice generation
- Print
- WhatsApp sharing
- Cart updates

The cashier should be able to complete common transactions with minimal interaction.

Do not add unnecessary animations that slow down POS.

---

# 59. BARCODE SCANNER COMPATIBILITY

Support common barcode scanner workflows.

A scanner should be able to input barcode data into the appropriate field without requiring complicated manual interaction.

Support:

- USB scanner
- Bluetooth scanner where supported
- Mobile camera scanning where already supported
- Manual barcode entry

Do not break normal keyboard input.

---

# 60. MOBILE CAMERA POS SUPPORT

Where the existing P4/P5 architecture supports mobile POS scanning:

- Use the device camera appropriately.
- Provide permission handling.
- Provide clear scanning state.
- Handle denied permissions gracefully.
- Do not continuously access the camera unnecessarily.
- Stop the camera when leaving the scanner.
- Avoid battery-heavy background camera usage.

---

# 61. WHATSAPP BILLING RELIABILITY

Maintain and improve the P2 WhatsApp billing feature.

Ensure:

- Correct invoice generated
- Correct customer number
- Correct branch information
- Correct business branding
- Correct logo
- Correct invoice details
- Correct amount
- Correct payment status

Do not claim a message was successfully delivered unless the actual integration confirms the relevant state.

Maintain retry/error handling where applicable.

---

# 62. WHITE-LABEL BRANDING CONSISTENCY

P2 white-label branding must remain centralized.

Branding should correctly propagate to:

- Login
- Dashboard
- Sidebar
- Header
- POS
- Invoice
- PDF
- WhatsApp bill
- Reports
- Emails where applicable
- Favicon where applicable
- Browser title where applicable
- Customer-facing documents

Organization-level branding should be inherited by branches unless a permitted branch override exists.

Do not hardcode "MedCare" into reusable white-label components.

---

# 63. LOGO STORAGE & UPDATE RELIABILITY

Logo updates must be centralized and reliable.

When a logo changes:

1. Upload securely.
2. Validate file type.
3. Validate size.
4. Store reference.
5. Update organization branding.
6. Invalidate relevant cache.
7. Refresh UI.
8. Ensure future invoices use the new logo.
9. Ensure WhatsApp/PDF documents use the new logo.
10. Ensure old cached logo is not incorrectly displayed.

Avoid storing unnecessary duplicate copies.

---

# 64. SYSTEM SETTINGS ARCHITECTURE

Organize settings into logical categories:

- Organization
- Branch
- Branding
- POS
- Billing
- Tax
- Inventory
- Purchase
- Customer
- Supplier
- Staff
- Notifications
- Printer
- Backup
- Security
- Integrations

Do not put every setting on one extremely long page.

---

# 65. SEARCH INDEX / CACHE STRATEGY

For very large datasets, consider a search index or optimized database search strategy.

The solution must be appropriate for the actual backend/database being used.

Do not add unnecessary external infrastructure if the existing database can efficiently handle the search.

Use caching only where it provides measurable benefit.

Never cache sensitive or branch-specific data in a way that can cause cross-branch leakage.

---

# 66. RATE LIMITING

Protect APIs from abuse and accidental request floods.

Apply appropriate rate limits to:

- Login
- OTP/authentication if present
- Search APIs
- Export APIs
- Report generation
- Password operations
- Sensitive administrative APIs

Do not rate-limit normal POS operations so aggressively that legitimate pharmacy transactions fail.

---

# 67. REQUEST CANCELLATION

Autocomplete and other rapidly changing requests should support cancellation or stale-response protection.

Example:

User searches:

par

then immediately:

para

If the response for "par" arrives after "para", it must not overwrite the "para" results.

Always render the latest valid query.

---

# 68. PAGINATION + VIRTUALIZATION

Large lists should never load all records at once.

Use:

- Server-side pagination
- Cursor pagination where appropriate
- Virtualized lists for very large UI collections

Apply to:

- Staff
- Customers
- Medicines
- Invoices
- Purchases
- Suppliers
- Stock
- Audit logs
- Notifications
- Reports

---

# 69. LAZY LOADING

Load heavy modules only when required.

Examples:

- Reports
- Analytics
- Charts
- Large tables
- PDF generation interface
- Advanced settings

Do not make the initial dashboard load every possible module.

---

# 70. FRONTEND STATE MANAGEMENT

Audit the existing state-management architecture.

Avoid:

- Duplicate server state
- Stale data
- Excessive global state
- Unnecessary re-renders
- Race conditions

Use appropriate server-state caching and invalidation.

After an operation such as:

Create Invoice
↓
Update relevant cached data
↓
Refresh only affected areas

Do not reload the entire application unnecessarily.

---

# 71. DATA REFRESH STRATEGY

Use targeted refresh/invalidation.

Example:

After stock purchase:

Do not refresh every module.

Refresh:

- Relevant inventory
- Relevant medicine
- Purchase list
- Dashboard metrics where needed

This improves perceived performance.

---

# 72. SECURITY-AWARE LOGGING

Logs must never expose:

- Passwords
- Authentication tokens
- Secret API keys
- Payment secrets
- Sensitive personal information unnecessarily

Use redaction.

---

# 73. API RESPONSE STANDARDIZATION

Standardize API responses.

For example, conceptually:

Success:
- success
- data
- metadata
- request ID

Error:
- success
- error code
- safe message
- request ID

Do not expose internal stack traces.

This will make frontend error handling much more consistent.

---

# 74. DATABASE MIGRATION SAFETY

Before changing the database:

- Inspect existing schema.
- Create safe migrations.
- Preserve existing data.
- Add new fields carefully.
- Backfill data safely.
- Add indexes without unnecessarily blocking production.
- Avoid destructive migrations unless absolutely required.
- Provide rollback strategy where possible.

Never drop an existing table or column simply because a new structure is preferred.

---

# 75. DATA MIGRATION / BACKWARD COMPATIBILITY

If P1–P5 already contain data:

- Migrate existing data safely.
- Preserve existing UUIDs where possible.
- Do not create duplicate medicines.
- Do not duplicate customers.
- Do not duplicate suppliers.
- Do not break existing invoices.
- Do not invalidate existing branch relationships.
- Do not change historical financial data incorrectly.

Run validation after migration.

---

# 76. TESTING REQUIREMENT

Before declaring P6 complete, test:

## Functional

- Branch creation
- Branch switching
- Staff management
- Medicine management
- Purchase
- Sales
- Returns
- Stock transfer
- Customer
- Supplier
- Credit ledger
- Cash register
- Reports
- Search
- Notifications
- Backup
- Branding
- WhatsApp billing

## Security

Test that:

- Branch A cannot access Branch B.
- Organization A cannot access Organization B.
- Cashier cannot perform Manager-only operations.
- Manager cannot perform Super Admin-only operations.
- Unauthorized UUIDs cannot expose records.

## Performance

Test:

- Large medicine dataset
- Large customer dataset
- Large invoice dataset
- Large staff dataset
- Multiple simultaneous searches
- Multiple concurrent POS operations
- Large report generation

## Responsive

Test:

- Android Chrome
- iOS Safari
- Android tablet
- iPad
- Desktop Chrome
- Desktop Edge
- Other supported browsers

## Theme

Test:

- Light mode
- Dark mode

---

# 77. LOAD TESTING

Do not simply claim that the application supports 10,000 concurrent users.

Actually identify bottlenecks.

Perform realistic load testing where the environment allows it.

Measure:

- Requests per second
- Average response time
- P95 latency
- P99 latency
- Error rate
- Database CPU
- Database connections
- Memory
- Cache performance
- Queue performance

Optimize based on actual measurements.

The goal is:

High concurrency
+
Predictable performance
+
Graceful degradation
+
No data corruption

---

# 78. GRACEFUL DEGRADATION

If a non-critical service fails:

Example:

Analytics service unavailable

The POS should still work.

If notification service fails:

Billing should not necessarily fail.

If report generation is busy:

POS should remain responsive.

Separate critical transaction paths from non-critical background services.

---

# 79. TRANSACTION PRIORITY

Prioritize:

1. Authentication
2. POS billing
3. Payment
4. Inventory integrity
5. Stock transfer
6. Customer/supplier ledger
7. Critical business operations
8. Reports
9. Analytics
10. Non-critical notifications

Do not allow heavy analytics/report queries to block critical POS operations.

---

# 80. SYSTEM-WIDE UUID ARCHITECTURE

Continue the UUID strategy from P4/P5.

Use UUIDs consistently for important entities.

Examples:

- Organization UUID
- Branch UUID
- User UUID
- Staff UUID
- Medicine UUID
- Batch UUID
- Customer UUID
- Supplier UUID
- Invoice UUID
- Purchase UUID
- Transfer UUID
- Payment UUID
- Expense UUID
- Audit UUID

Do not use names as primary identifiers.

Do not assume a UUID grants access.

Always validate authorization.

---

# 81. NO HARDCODED BUSINESS DATA

Remove or avoid hardcoded:

- Business name
- Logo
- Address
- Phone
- Email
- Branch name
- Tax information
- Invoice prefix
- Staff
- Medicine
- Supplier
- Customer

All such data should come from the appropriate organization/branch/database configuration.

This is especially important for white-label SaaS deployment.

---

# 82. CLEAN CODE ARCHITECTURE

Keep responsibilities separated.

Prefer:

UI
↓
API
↓
Controller
↓
Service
↓
Business Logic
↓
Repository/Data Access
↓
Database

Avoid putting complex business logic directly inside UI components.

Avoid duplicating business rules across multiple pages.

Create reusable services for:

- Search
- Permissions
- Branch context
- Organization context
- Inventory
- Billing
- Notifications
- Audit
- Branding

---

# 83. BRANCH CONTEXT SERVICE

Create one reliable source of truth for the active branch.

The branch context should be available to relevant modules without manually duplicating branch-selection logic everywhere.

When branch changes:

- Update UI
- Update API context
- Clear incompatible cached data
- Reload branch-specific information
- Prevent stale data from previous branch

Example:

Branch 02
↓
Switch to Branch 04
↓
Clear Branch 02 operational cache
↓
Load Branch 04 data

This is critical to prevent accidental cross-branch display.

---

# 84. ORGANIZATION CONTEXT SERVICE

Similarly maintain a secure organization context.

Every organization-scoped operation must use the authenticated organization context.

Never allow the client to freely choose an organization UUID and assume it is valid.

---

# 85. ADMIN COMMAND CENTER

Create a separate, professional Super Admin command center if the existing architecture allows it.

It should focus on:

- Organization overview
- Branch management
- Branch switching
- Staff
- Permissions
- System health
- Security
- Audit
- Backup
- Global reports
- Branding
- Integrations
- Feature flags

Normal branch users should not see Super Admin controls.

---

# 86. SUPER ADMIN BRANCH OVERVIEW

Show all branches in a clean management interface.

Each branch should show:

- Branch name
- Branch code
- Status
- Active staff
- Today's sales
- Stock value
- Low stock
- Expiry alerts
- Pending approvals
- Cash status
- Last activity

Allow authorized Super Admin actions:

- Open branch
- Manage branch
- Manage staff
- View reports
- Configure branch
- Activate/deactivate where appropriate

---

# 87. BRANCH DEACTIVATION SAFETY

Never allow accidental destructive branch deletion.

Prefer:

Active
↓
Suspended
↓
Archived

Before deactivation, clearly warn about:

- Active staff
- Pending invoices
- Pending stock transfers
- Outstanding supplier balances
- Customer credit
- Inventory
- Unclosed cash registers

Historical financial records should remain preserved.

---

# 88. NOTIFICATION PRIORITY

Support notification priority:

- Critical
- High
- Medium
- Low
- Informational

Examples:

Critical:
Database/backup failure

High:
Expired stock
Security alert

Medium:
Low stock
Pending approval

Low:
General information

---

# 89. REPORT EXPORT PERFORMANCE

For large reports:

User selects report
↓
System creates export job
↓
Background worker processes it
↓
User receives completion notification
↓
User downloads generated file

Do not freeze the browser for large exports.

---

# 90. SEARCH RESULT UX

Search suggestions should be professional and information-rich without becoming cluttered.

Example:

Medicine
Paracetamol 500mg

Brand:
XYZ Pharma

Stock:
120

Branch:
Main Branch

Barcode:
XXXXXXXX

Use keyboard navigation on desktop:

Arrow Up
Arrow Down
Enter
Escape

On mobile:

- Large touch targets
- Proper viewport positioning
- No clipping
- No accidental horizontal overflow
- Easy selection

---

# 91. NO AI-STYLE DECORATIVE UI

Do not add unnecessary:

- AI-generated-looking icons
- Emoji
- Decorative gradients everywhere
- Excessive glowing effects
- Excessive animations
- Artificial futuristic elements

The ERP should feel like a serious professional enterprise business application.

---

# 92. ANIMATION POLICY

Use subtle functional animations only.

Allowed:

- Loading transitions
- Dropdown transitions
- Modal transitions
- Success feedback
- Skeleton loading
- Progress indicators

Avoid animations that:

- Delay interaction
- Consume unnecessary resources
- Distract cashiers
- Cause iOS scrolling problems
- Reduce accessibility

---

# 93. MOBILE-FIRST BUSINESS OPERATIONS

Important actions should remain easy to perform on mobile.

Examples:

- Search medicine
- Scan barcode
- Create sale
- Select customer
- View invoice
- Share invoice
- Check stock
- Check expiry
- Check branch
- Approve request
- Switch branch for Super Admin

Do not force desktop-only workflows.

---

# 94. USER EXPERIENCE FOR SLOW NETWORKS

When network latency is high:

- Show clear loading state.
- Disable duplicate submission.
- Preserve entered data.
- Retry safe requests.
- Show request status.
- Avoid blank screens.
- Avoid unnecessary full-page reloads.

Never make users unsure whether a payment or invoice was submitted.

---

# 95. DUPLICATE SUBMISSION PROTECTION

Protect all important buttons against double-click/tap.

Examples:

- Create invoice
- Save purchase
- Make payment
- Approve transfer
- Receive transfer
- Add expense
- Submit refund
- Send WhatsApp bill

Use:

- UI loading state
- Backend idempotency
- Database constraints
- Transaction logic

The backend remains the final protection.

---

# 96. DATA INTEGRITY CHECKS

Add internal consistency checks for important records.

Examples:

Invoice total
=
Item totals
-
Discount
+
Applicable taxes

Stock after transaction
=
Previous stock
+
Stock movement

Customer balance
=
Credit
-
Payments
+
Adjustments

Supplier balance
=
Purchases
-
Payments
-
Returns
+
Adjustments

These checks should help detect corrupted or inconsistent data.

---

# 97. ADMIN DATA REPAIR TOOLS

If technically appropriate, create controlled Super Admin tools for resolving exceptional data problems.

Examples:

- Recalculate invoice totals
- Rebuild stock summary from ledger
- Recalculate customer balance
- Recalculate supplier balance
- Repair stale cache
- Retry failed background job

These actions must:

- Require authorization
- Require confirmation
- Create audit records
- Never silently alter historical data

---

# 98. SYSTEM DOCUMENTATION

Document important architecture decisions.

Maintain documentation for:

- Database relationships
- Branch isolation
- Organization isolation
- Authentication
- Roles
- Permissions
- Search
- Stock ledger
- Stock transfer
- Invoice numbering
- Backup
- Deployment
- Recovery
- API conventions

This will make future maintenance easier.

---

# 99. FINAL REGRESSION TEST

After P6 implementation, verify every existing module.

Do not assume that a successful build means the ERP is working.

Test:

- Login
- Logout
- Dashboard
- POS
- Medicine
- Inventory
- Batch
- Purchase
- Purchase Orders
- Sales
- Invoice
- Sales Return
- Suppliers
- Customers
- Expenses
- Reports
- Opening Stock
- Import
- Settings
- Branches
- Staff
- Roles
- Branding
- WhatsApp bill
- Search
- Dark mode
- Light mode
- Mobile
- Tablet
- Desktop
- iOS Safari
- Android Chrome

---

# 100. FINAL ACCEPTANCE CRITERIA

P6 should only be considered complete when:

- P1 remains fully functional.
- P2 remains fully functional.
- P3 remains fully functional.
- P4 remains fully functional.
- P5 remains fully functional.
- Branch data remains isolated.
- Organization data remains isolated.
- Super Admin can safely operate across authorized branches.
- Branch staff cannot access unauthorized branches.
- Medicine master and branch inventory are properly separated.
- Stock transfers are transaction-safe.
- Purchases are branch-aware.
- Supplier ledgers are branch-aware.
- Customer recognition works without unnecessary duplication.
- All relevant search boxes use the reusable autocomplete system.
- Search remains fast with large datasets.
- Cash registers are branch-specific.
- Cashier shifts are tracked.
- Sensitive actions have approval controls.
- Discounts are enforced server-side.
- Audit logs exist for critical operations.
- Notifications respect branch and role permissions.
- Backup and recovery mechanisms are reliable.
- POS remains responsive during heavy background activity.
- Large reports do not block normal ERP operations.
- UUID-based routing remains intact.
- Authorization is enforced server-side.
- No sensitive information is leaked through URLs or APIs.
- Light mode works correctly.
- Dark mode works correctly.
- Android works correctly.
- iOS Safari works correctly.
- Tablets work correctly.
- Desktop works correctly.
- No unnecessary horizontal scrolling exists.
- No unnecessary AI-style decorative UI has been introduced.
- No existing business functionality has been broken.

---

# MOST IMPORTANT IMPLEMENTATION RULE

Do not treat P6 as a simple UI feature update.

This is an **architecture, performance, security, data-integrity, scalability and enterprise UX upgrade**.

Before changing anything:

1. Inspect the current codebase.
2. Identify existing architecture.
3. Identify existing P1–P5 implementations.
4. Identify database relationships.
5. Identify current branch/organization isolation.
6. Identify reusable components.
7. Identify performance bottlenecks.
8. Identify security risks.
9. Create a safe implementation plan.
10. Implement incrementally.
11. Run regression tests after each major module.
12. Verify that existing data and functionality remain intact.
13. Optimize only after measuring actual bottlenecks.
14. Do not introduce unnecessary dependencies.
15. Do not replace working architecture merely for the sake of rewriting it.

The final result should feel like a **professional, scalable, secure, multi-branch pharmacy ERP/POS product**, not a collection of disconnected features.